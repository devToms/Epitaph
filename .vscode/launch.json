{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Docker: Attach to PHP",
        "type": "php",
        "request": "launch",
        "port": 9003,
        "pathMappings": {
          "/var/www/html": "${workspaceFolder}"
        }
      }
    ]
  }
  